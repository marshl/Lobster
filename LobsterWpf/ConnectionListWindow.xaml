<Window x:Class="LobsterWpf.ConnectionListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LobsterWpf"
        mc:Ignorable="d"
        Title="ConnectionListWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <Label x:Name="directoryLabel" Width="170" HorizontalAlignment="Left" VerticalAlignment="Center" Content="[DefaultDirectory]" Margin="0,12"/>
        <ListBox Grid.Row="1" Name="connectionListBox" ItemsSource="{Binding databaseConfigList}">
            <ListBox.ItemTemplate>
                <DataTemplate >
                    <StackPanel>
                        <TextBlock>Name</TextBlock>
                        <TextBlock Text="{Binding Path=Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button x:Name="connectButton" Content="Connect" HorizontalAlignment="Right" Margin="0,10,90.333,0" Grid.Row="2" VerticalAlignment="Top" Width="75"/>
        <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Right" Margin="0,10,10.333,0" Grid.Row="2" VerticalAlignment="Top" Width="75" RenderTransformOrigin="-0.567,0.738" Click="cancelButton_Click"/>
        <Button x:Name="button" Content="Change Directory" HorizontalAlignment="Right" Margin="0,13,10.333,17" VerticalAlignment="Center" Width="108"/>
        <ComboBox x:Name="comboBox" HorizontalAlignment="Left" Margin="298,120,-124.667,0" Grid.Row="1" VerticalAlignment="Top" Width="120"/>
    </Grid>
</Window>
