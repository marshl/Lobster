<Window x:Class="LobsterWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LobsterWpf"
        mc:Ignorable="d"
        Title="Lobster" Height="600" Width="800" Loaded="Window_Loaded">
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Menu>
                <MenuItem Header="_File">
                    <MenuItem Header="Connections" Click="MenuItem_Click" />
                    <Separator/>
                    <MenuItem Header="_Exit" Click="MenuItem_Click_1" />
                </MenuItem>
            </Menu>
        </Grid>
        <Grid Grid.Row="1" IsEnabled="False" Name="ConnectionContainer">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ListBox Name="clobTypeListBox" Grid.Column="0" Grid.Row="1" ItemsSource="{Binding ClobTypes }" SelectionChanged="clobTypeListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Directory}" FontStyle="Italic"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <GridSplitter x:Name="gridSplitter" HorizontalAlignment="Left" Grid.Column="1" Width="5"/>
            <TabControl x:Name="tabControl" Grid.Column="2" Margin="10">
                <TabItem Header="Local Files" >
                    <Grid Background="#FFE5E5E5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition MaxWidth="50"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <GridSplitter x:Name="gridSplitter1" HorizontalAlignment="Left" Width="5" Grid.Column="2"/>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="25"/>
                            </Grid.RowDefinitions>
                            <TreeView Name="localFileTreeView" ItemsSource="{Binding RootFile}" Grid.Row="0">
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate DataType="{x:Type local:FileNodeView}" ItemsSource="{Binding Children}">
                                        <HierarchicalDataTemplate.Resources>
                                            <Style TargetType="{x:Type StackPanel}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </HierarchicalDataTemplate.Resources>
                                        <StackPanel Orientation="Horizontal">
                                            <Image Source="img/Folder_Open.png" Height="24"/>
                                            <TextBlock Text="{Binding Path=Name}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>

                            <CheckBox x:Name="showReadOnlyFilesCheckbox" Grid.Row="1" Content="Show Readonly Files" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" Checked="hideReadonlyCheckbox_Checked" Unchecked="hideReadonlyCheckbox_Unchecked" IsChecked="{Binding Path=ShowReadOnlyFiles, Mode=TwoWay}"/>
                        </Grid>
                        <StackPanel>
                            <Button x:Name="pushButton" Grid.Column="0" MaxHeight="50" Margin="0">
                                <StackPanel MaxHeight="50">
                                    <Image Source="img/112_UpArrowShort_Orange_48x48_72.png" VerticalAlignment="Top" Height="30"/>
                                    <TextBlock HorizontalAlignment="Center" MinHeight="15" VerticalAlignment="Bottom">Push</TextBlock>
                                </StackPanel>
                            </Button>
                            <Button x:Name="diffButton" Grid.Column="0" MaxHeight="50">
                                <StackPanel MaxHeight="50">
                                    <Image Source="img/041_Sort_48x48_72.png" VerticalAlignment="Top" Height="30"/>
                                    <TextBlock HorizontalAlignment="Center" MinHeight="15" VerticalAlignment="Bottom">Diff</TextBlock>
                                </StackPanel>
                            </Button>
                            <Button x:Name="exploreButton" Grid.Column="0" MaxHeight="50">
                                <StackPanel MaxHeight="50">
                                    <Image Source="img/Folder_Open.png" VerticalAlignment="Top" Height="30"/>
                                    <TextBlock HorizontalAlignment="Center" MinHeight="15" VerticalAlignment="Bottom">Explore</TextBlock>
                                </StackPanel>
                            </Button>
                            <Button x:Name="insertButton" Grid.Column="0" MaxHeight="50">
                                <StackPanel MaxHeight="50">
                                    <Image Source="img/112_Plus_Green_48x48_72.png" VerticalAlignment="Top" Height="30"/>
                                    <TextBlock HorizontalAlignment="Center" MinHeight="15" VerticalAlignment="Bottom">Insert</TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <Border BorderThickness="1" Background="White" CornerRadius="5" BorderBrush="Black">
                                <Expander x:Name="detailsExpander"  IsExpanded="True" >
                                    <Expander.Header>
                                        <TextBlock>Details</TextBlock>
                                    </Expander.Header>
                                    <StackPanel Grid.Column="2">
                                        <Label>[Filename]</Label>
                                        <Label>[Size]</Label>
                                    </StackPanel>
                                </Expander>
                            </Border>
                            <Border BorderThickness="1" Background="White" CornerRadius="5" BorderBrush="Black">
                                <Expander x:Name="istoryExpander" IsExpanded="True" Background="White">
                                    <Expander.Header>
                                        <StackPanel>
                                            <TextBlock>Change Log</TextBlock>
                                        </StackPanel>
                                    </Expander.Header>
                                    <StackPanel Grid.Column="2">
                                        <Label>Hello</Label>
                                        <Label>World</Label>
                                    </StackPanel>
                                </Expander>
                            </Border>
                        </StackPanel>

                    </Grid>
                </TabItem>
                <TabItem Header="Database Only Files">
                    <Grid Background="#FFE5E5E5"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
