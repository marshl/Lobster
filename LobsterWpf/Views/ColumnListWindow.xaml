<Window x:Class="LobsterWpf.Views.ColumnListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LobsterWpf.Views"
        mc:Ignorable="d"
        Title="Columns" Height="300" Width="400" MinWidth="300" MinHeight="300" ShowInTaskbar="False" Background="{StaticResource BackgroundKey}" Icon="../Resources/Images/cartoon-lobster.ico" >
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <ListBox Name="columnListBox" Grid.Row="0" ItemsSource="{Binding ColumnList}">
      <ListBox.ItemTemplate>
        <DataTemplate DataType="ColumnView">
          <StackPanel>
            <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0" Orientation="Horizontal">
        <Button Margin="5" Width="75" HorizontalAlignment="Left" Click="NewButton_Click">New</Button>
        <Button Margin="5" Width="75" HorizontalAlignment="Left" Click="EditButton_Click">Edit</Button>
        <Button Margin="5" Width="75" HorizontalAlignment="Left" Click="DeleteButton_Click">Delete</Button>
      </StackPanel>
      <Button Grid.Column="1" Margin="5" Width="75" HorizontalAlignment="Right" Click="CloseButton_Click">Close</Button>
    </Grid>
  </Grid>
</Window>
